{
  "name": "nesto",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --project=chromium",
    "test:portfolio": "npx playwright test --project=chromium-auth-setup --ui",
    "test:login": "npx playwright test --project=chromium-navigate-setup --ui",
    "test:ui": "npx playwright test --project=chromium --ui",
    "test:headed": "npx playwright test --project=chromium --headed",
    "report:show": "npx playwright show-report",
    "report:allure": "allure generate ./allure-results --clean -o ./allure-report && allure open ./allure-report",
    "lint": "npx eslint . --ext .ts",
    "lint:fix": "npx eslint . --ext .ts --fix",
    "format": "npx prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@faker-js/faker": "^10.2.0",
    "@playwright/mcp": "^0.0.54",
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3",
    "allure-playwright": "^3.4.3",
    "typescript": "^5.9.3"
  }
}
