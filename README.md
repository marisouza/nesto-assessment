# Installation

Run `npm install` to install dependencies.

## Running Tests

- Headless execution with chromium `npm run test`.
- Headed execution with chromum `npm run test:headed`.
- Show generate and show report `npm run report:allure`.

to ran different locales (en or fr only) LANGUAGE=language value
example: LANGUAGE=fr npm run test:ui
